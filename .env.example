# Copy this file to .env and replace the placeholder values before
# running `docker compose up`.
#
# GOOGLE_OAUTH_CLIENT_ID should match the Web Client configured in Google Cloud.
# The same value hydrates the Docker demo automatically; no other files need
# manual editing.

APP_LISTEN_ADDR=:8080
APP_COOKIE_DOMAIN=localhost
APP_GOOGLE_WEB_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
APP_JWT_SIGNING_KEY=change-me-in-prod
APP_ENABLE_CORS=true
APP_CORS_ALLOWED_ORIGINS=http://localhost:8000
APP_DEV_INSECURE_HTTP=true
# Optional override when the backend lives on a different host/port than the
# default localhost mapping used by docker-compose.
# DEMO_AUTH_BASE_URL=http://localhost:8080
# APP_DATABASE_URL=postgres://user:pass@host:5432/tauth?sslmode=disable
